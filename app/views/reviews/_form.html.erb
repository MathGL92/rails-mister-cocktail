<%= simple_form_for [ @cocktail, @review ] do |f| %>
    <%= f.input :user %>
    <%= f.input :content %>
    <%= f.input :rating, collection: 0..5 %>
    <%= f.button :submit, class: 'btn btn-outline-light' %>
<% end %>
